const message = "Hello World!";
// the base problem, on javascript there's no
// error until runtime because of the lack of static typing

// TypeError: message is not a function
console.log(message());
